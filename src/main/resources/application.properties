# Configurações do banco de dados
spring.datasource.url=jdbc:postgresql://localhost:5432/projectvet
spring.datasource.username=postgres
spring.datasource.password=123456

# Configuração do Hibernate

spring.datasource.driverClassName=org.postgresql.Driver
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=update

#Secrete
api.security.token.secret=${JWT_SECRET:my-secret-key}

#Envio de email para da Senha
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=fabricioemanuel20@gmail.com
spring.mail.password=opvy knue hjqa jmot
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

# Salvar imagens no disco
contato.disco.raiz = C:/Users/thiag/Desktop
contato.disco.diretorio-imagens = projectvet/imgs
spring.web.resources.static-locations = file:${contato.disco.raiz}/${contato.disco.diretorio-imagens}

